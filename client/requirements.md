## Packages
chess.js | Chess logic validation and state management
react-chessboard | React component for rendering the chess board
framer-motion | Smooth animations for UI elements
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes efficiently

## Notes
WebSocket connection required at /ws endpoint
App assumes a "dark mode" aesthetic by default for that premium gaming feel
Mobile-first design requires touch-friendly targets
